# Important! Replace all bracketed text with environment specific values. 

# Here, Doctor Name can be any pretty format you want. 
# Environment should be one of: Development | Staging | Production
name: Deploy WP - Justin Bonvallet - [Production]
on:
  push:
    branches:      
      - bonvalletjprod
jobs:
  deploy:    
    runs-on: ubuntu-latest    
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Sync
        uses: Burnett01/rsync-deployments@4.1
        with:
          switches: -avzr --delete --filter=". ./.rsync-filter"
          path: cms/          
          remote_host: 35.236.219.140
          remote_port: 24287
          remote_path: /www/justinbonvalletcms_642/public/wp-content
          remote_user: justinbonvalletcms
          remote_key: ${{ secrets.KINSTA_SSH_DEPLOY_PRIVATE_KEY }}