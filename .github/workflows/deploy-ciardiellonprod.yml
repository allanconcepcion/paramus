name: Deploy - Nicholas Ciardiello [Production]
on:
  push:
    branches:
      - ciardiellonprod  
jobs:
  deploy:    
    runs-on: ubuntu-latest    
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Sync
        uses: Burnett01/rsync-deployments@4.1
        with:
          switches: -avzr --delete --filter=". ./.rsync-filter"
          path: cms/          
          remote_host: 35.236.219.140
          remote_port: 60395
          remote_path: /www/nicholasciardiellocms_345/public/wp-content
          remote_user: nicholasciardiellocms
          remote_key: ${{ secrets.KINSTA_SSH_DEPLOY_PRIVATE_KEY }}          

